INSTALL
	firefox for geolocation
	nodejs
	mongodb v3.2 -> "C:\Program Files\MongoDB\Server\3.2\bin\mongod.exe"
	RoboMongo
	VS Code (change SaveAs keyboard shortcut to SaveAll)

COMMANDS
	npm install
	npm install mongodb
	npm install -g tsd
	tsd install node
	tsd install express
	run.bat -> run.base.bat -> mongod.exe && node app\index.js
